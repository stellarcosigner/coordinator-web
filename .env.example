# Base URL of the coordinator-api backend. In production this must point at
# your deployed API origin (e.g. https://api.your-domain.com). The API must
# allow this frontend's origin via its CORS_ORIGIN setting.
#
# If unset, the app defaults to http://localhost:3000 (the local API default).
VITE_API_URL=http://localhost:3000
